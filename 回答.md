@workspace 请根据 `CHANGELOG.md` 中的实验数据，创建一个全新的实验记录文档 `EXPERIMENT_LOG.md`。

**核心要求：**
1. **纯净数据**：只提取实验配置和测试结果（表格），**不要**包含任何代码修改、文件路径或功能开发描述。
2. **倒叙排列**：按照时间从新到旧排列（最新的实验 EXP-006 在最上面）。
3. **固定格式**：严格遵守下方的文档结构，包括顶部的 HTML 锚点。

---

### 第一步：创建文档结构

请创建 `EXPERIMENT_LOG.md`，文件头部必须严格包含以下内容：

```markdown
# 🧪 PUAD 实验数据记录 (Experiment Log)

> 本文档仅记录关键实验配置与测试指标 (AUROC/PRO)。
> ❌ 不记录代码变更 | ✅ 记录训练参数 | ✅ 记录环境差异

---

(此处开始插入倒叙的实验记录...)
第二步：提取并填充数据 (从新到旧)
请从 CHANGELOG.md 中提取以下 6 次关键实验的数据，并按 EXP-006 -> EXP-001 的顺序写入文档。

[EXP-006] 合成异常负样本训练 (完整评估)

数据源: 2026-01-03 "完整评估结果（合成异常负样本训练模型）"

关键特征: PRO Score 大幅提升 (0.0955), Structural AUROC (0.9434)

配置: Synthetic Anomaly Enhanced (Student Only), 100% Frequency

[EXP-005] 合成异常负样本训练 (训练阶段)

数据源: 2026-01-03 "训练结果（合成异常负样本增强）"

关键特征: 训练耗时约 216 分钟, 平均 AUROC 0.8895

配置: Iterations: 70,000, Loss Weight: 1.0, Online Generation

[EXP-004] 重训练模型 + PUAD 完整评估

数据源: 2026-01-01 "重训练模型 + PUAD 完整评估结果"

关键特征: 平均 PRO 0.0855, PUAD AUROC 0.9158

配置: Retrained EfficientAD + PUAD (Student Feature Extractor)

[EXP-003] EfficientAD 重训练基线

数据源: 2026-01-01 "重新训练后的 EfficientAD 性能"

关键特征: 平均 AUROC 0.8794 (低于原版), 耗时 175 分钟

配置: Re-trained EfficientAD (S), ImageNet Negative only

[EXP-002] PRO 指标基准 (修复后)

数据源: 2025-12-31 "PRO 指标评估结果 (修复后)"

关键特征: 首次建立 PRO 基线 (0.0844), GT Masks 数量 993

配置: Original Pre-trained Model + Fix GT Path

[EXP-001] 项目初始化基准

数据源: 2025-12-31 "基准测试结果 (Baseline Results)"

关键特征: PUAD AUROC 0.9318 (历史最高)

配置: Original Pre-trained EfficientAD + PUAD
每个实验节点的标准格式 (Template)
请确保每个实验节点都遵循此格式：
## [EXP-XXX] 实验名称
- **日期**: YYYY-MM-DD
- **模型状态**: (例如：Synthetic Anomaly Enhanced / Original Pre-trained)
- **关键配置**: (简要列出 2-3 个关键参数，如 Iterations, Augmentation, Loss Weight)
- **数据表**:
(在此处插入 Markdown 表格，保留 类别/AUROC/PRO/GT数 等关键列)
- **📉 关键结论**:
(用 1-2 句话总结该实验的核心发现，例如：PRO 相比上一版本提升了 13%。)
请现在执行并输出完整的 EXPERIMENT_LOG.md 文件内容。